Задачи по Javascript
Алгоритмы
Задача №1 Про блины.
К сожалению я не сумел написать код к этой задаче. Но могу описать алгоритм.
NB! Т.к. я только приступил к изучению объектов по учебнику learn.javascript.ru, 
я еще не знаком полностью с синтаксисом, поэтому возможно некорректное использование скобок.

var pan =2;
var pancake{value:3, side:2}

 При оптимальном распределении необходимо 3 итерации 
1. pancake[1,1]; pancake[2,1];
2. pancake[1,2]; pancake[3,1];
3. pancake[2,2]; pancake[3,2];

Т.к. у нас две стороны и 2 сковороды, то общее время равно 2 минутам при жарке одного блина и кол-ву блинов в остальных случаях.

Понимаю, что обладая знаниями работы с массивами, решение гораздо проще. 
Я решил, как смог:

var timer = 0;
var pan = 2;
var pancake{value:3, side:2}

function checkTime(pancake) {
//кол-во иттераций - в нашем случае 2 сковороды,поэтому делим на 2.
var ratio = (pancake[value] / 2);  

//если только 1 блин
if (pancake[value]=1 {
  for (i=1; i=2; i++) {
   pancake[1,i];
  timer +=1; //счётчик тикает на жарке каждой стороны
  }
  
//если четное количество блинов
} else {
while (ratio ^ 0 === ratio) { 
  for (i = 2; i =< pancake[value]; i+=2) {
      for (j=1; j=2; j++) {
        pancake[i-1,j]; 
        pancake[i,j];
	timer +=1; //счётчик тикает на жарке каждой стороны
       }
  }
} 

//если блинов нечетное количество - дожариваем блины по хитрой схеме
if (ratio ^ 0 === ratio)  {
return 'пожарили четное количество блинов'
} else {
for (i = (pancake[value]-2); i <= (pancake[value]-1); i+=2) {
      for (j=1; j=2; j++) {
        pancake[i-1,j]; 
        pancake[i,j];
	timer +=1; //счётчик тикает на жарке каждой стороны
       }
       
	pancake[i-2,2]; 
	pancake[i,1];
	timer +=1; //счётчик тикает на жарке каждой стороны
	pancake[i,2];
	pancake[i-1,2];
	timer +=1; //счётчик тикает на жарке каждой стороны
 }
return timer;
}
